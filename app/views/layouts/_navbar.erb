<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="<%= root_url %>">Home</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <!-- <li class="nav-item active">
        <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
      </li> -->
      <% if @cart.present? %>
        <li class="nav-item">
          <a class="nav-link" href="<%= cart_path(@cart) %>">Cart</a>
        </li>
      <% end %>
      <li class="nav-item">
        <a class="nav-link" href="/histories">My history</a>
      </li>
    </ul>
    <span class="navbar-text">
      <ul class="navbar-nav mr-auto">
        <% if current_user.present? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= edit_user_registration_path(current_user) %>">Me</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= destroy_user_session_path()%>" data-method="delete">Logout</a>
          </li>
        <% end %>
      </ul>
    </span>
  </div>
</nav>
